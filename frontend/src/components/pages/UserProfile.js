import React from 'react';
import { Link } from 'react-router-dom';
import { Typography } from '@material-ui/core'

function UserProfile() {
  return (
    <div>
      <Typography variant="h1" color="initial">User here!</Typography>
    </div>
  )
}

export default UserProfile
